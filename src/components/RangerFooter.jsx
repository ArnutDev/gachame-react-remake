export default function RangerFooter({ specialCount }) {
  return (
    <footer className="mt-4 py-2 border-top text-center">
      <p className="mb-0">ตัวพิเศษที่ได้ทั้งหมด: {specialCount}</p>
    </footer>
  );
}
